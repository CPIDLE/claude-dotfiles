> ⚠️ **Legacy 指令** — 建議改用 `/pm sync`，除同步外還有 Code Review、QA 等選項。
> `/pm sync 2` 等同 `/sc 1`（直接同步）。此指令仍可正常使用。

---

# SC — 進度同步（已遷移至 Google Workspace）

此指令原為 Slack Canvas 同步工具，現已遷移至 Google Docs + Google Chat。

**請改用 `/pm sync`**，功能完全涵蓋且已更新為 Google Workspace 整合。

| 舊指令 | 新指令 |
|--------|--------|
| `/sc` | `/pm sync` |
| `/sc 1` | `/pm sync 2` |
| `/sc 2` | `/pm sync`（選單中選擇） |

## 執行步驟

收到 `/sc` 時，顯示：
```
⚠️ /sc 已遷移至 Google Workspace。
💡 請改用 /pm sync（同步到 Google Doc + Chat Space）
   或 /pm sync 2（直接同步，跳過選單）
```
