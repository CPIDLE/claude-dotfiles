# 紅隊審核報告

## Metadata

| 項目 | 值 |
|---|---|
| 日期 | 2026-03-27 15:00 |
| 專案 | claude-dotfiles |
| Branch | master |
| 審核 Commits | `35c15a9`, `76f738b`, `fbd3f4b`, `6bb5ebf` |
| 審核範圍 | CLAUDE.md, commands/pm.md, commands/bye.md, commands/hello.md, commands/sc.md |
| 審核重點 | Chat Email→Webhook 遷移完整性、Google Doc→Sheet 遷移殘留、跨檔案一致性 |

---

## 總評：⚠️ REVISE

pm.md 的 Webhook 遷移和 Sheet 遷移在主體上完成良好，但 **pm.md 第 504 行殘留了一處 `Chat Email` 引用**，這是本次遷移的核心目標（email→webhook）卻未完全清除的殘留。此外，CLAUDE.md 將完整的 Webhook URL（含 API key 和 token）hardcode 在版本控制中，違反了同一份文件宣告的安全編碼底線。

---

## 自動掃描結果

### 殘留引用掃描

| 掃描模式 | 命中位置（commands/ 範圍） | 狀態 |
|---|---|---|
| `Chat Email` | pm.md:504 | 🔴 |
| `email-to-chat` | 零命中 | ✅ |
| `gmail-work` | 零命中 | ✅ |
| `gmail_send_email` | 零命中 | ✅ |
| `gmail_list_emails` | 零命中 | ✅ |
| `gmail_create_draft` | 零命中 | ✅ |
| `Google Doc`（commands/） | 零命中 | ✅ |
| `Doc ID`（commands/） | 零命中 | ✅ |
| `Doc URL`（commands/） | 零命中 | ✅ |
| `google_drive_fetch`（commands/） | 零命中 | ✅ |
| `replace_section`（commands/） | 零命中 | ✅ |

### 兩份 CLAUDE.md 比對

| 檔案 | 結果 |
|---|---|
| `repo/CLAUDE.md` vs `~/.claude/CLAUDE.md` | ✅ 完全一致（diff 無輸出） |

---

## 統計

| 類別 | 🔴 阻塞 | 🟠 中等 | 🟡 輕微 |
|---|---|---|---|
| 術語一致性 | 1 | 0 | 0 |
| 安全 | 1 | 0 | 0 |
| **合計** | **2** | **0** | **0** |

---

## 發現清單

### 🔴 阻塞

#### F-001：pm.md bye Step 5 殘留 `Chat Email`

- **編號**：F-001
- **位置**：commands/pm.md:504
- **嚴重度**：🔴
- **問題**：pm.md `/pm bye` 的 Step 5 寫「先執行 Step PRE 檢查（CLAUDE.md 中的 Dashboard Sheet、**Chat Email**、Apps Script Web App ID 是否已設定）」。`Chat Email` 是舊的 email-to-chat 術語，CLAUDE.md 關鍵 ID 表中已無此條目（已改為 `Chat Webhook`）。Step PRE 按此描述去檢查一個不存在的 `Chat Email` ID，結果為「未設定」，導致 Google 同步跳過。
- **驗證**：
  - CLAUDE.md:11 的關鍵 ID 表三行為：`Chat Webhook`、`Dashboard Sheet`、`Apps Script Web App`
  - pm.md:240（sync 的 Step PRE）正確寫為 `Dashboard Sheet`、`Chat Webhook`、`Apps Script Web App`
  - pm.md:504（bye Step 5）寫 `Chat Email`，與 pm.md:240 矛盾
  - pm.md:613（注意事項）正確寫為 `Dashboard Sheet、Chat Webhook、Apps Script Web App`
- **裁決**：MUST FIX — `/pm bye` 的 Step 5 會因找不到 `Chat Email` ID 而判定「未設定」，靜默跳過整個 Google 同步。此為 6bb5ebf 遷移的遺漏。

---

#### F-002：CLAUDE.md hardcode Webhook URL 含 API key 和 token

- **編號**：F-002
- **位置**：CLAUDE.md:11（兩份皆同）
- **嚴重度**：🔴
- **問題**：CLAUDE.md 第 11 行將完整的 Google Chat Webhook URL（含 `key=AIzaSyDdI0hCZtE6vySjMm-WEfRq3CPzqKqqsHI` 和 `token=WnDNMALPegG-JGsZjqeBpil0-BnwdgAimDvgJKP_0uQ`）直接 hardcode 在版本控制的檔案中。此 repo 是 public（GitHub URL：https://github.com/CPIDLE/claude-dotfiles）。任何人可取得此 URL 並向 Google Chat Space 發送任意訊息。
- **驗證**：
  - CLAUDE.md:99 明確寫「不 hardcode secrets、API keys、passwords — 用環境變數或 config」
  - CLAUDE.md:11 直接 hardcode 了 API key 和 auth token
  - 此 Webhook URL 允許任何持有者無需額外身份驗證即可發送訊息到 Chat Space
- **裁決**：MUST FIX — 公開 repo 中 hardcode 的 Webhook URL 允許任意第三方向 Chat Space 發送訊息。違反同一份文件的安全編碼底線（第 99 行）。已洩漏的 URL 應立即在 Google Chat Space 中 revoke 並重新產生。

---

## 未覆蓋風險

1. **pm-v2-design.md 完全過時**：此設計文件仍大量引用 Google Doc、Slack 術語，與 4 次 commit 後的實際狀態嚴重脫節。雖非執行檔案，但作為架構記錄已完全失效。不在本次審核範圍內，但值得注意。

2. **Apps Script Web App ID 也是 hardcode**：CLAUDE.md:13 的 Apps Script Web App ID 同樣 hardcode 在 public repo。雖然 Apps Script Web App 的安全模型不同於 Webhook URL（Web App 有獨立的存取控制），但暴露部署 ID 仍降低了攻擊面的隱蔽性。

---

## 評分

| 面向 | 分數 (1-5) | 權重 | 說明 |
|---|---|---|---|
| 內部一致性 | 3 | 30% | pm.md:504 與 pm.md:240 和 CLAUDE.md 矛盾 |
| 遷移完整性 | 4 | 25% | 絕大部分完成，僅 1 處殘留 |
| 安全 | 1 | 25% | 公開 repo hardcode Webhook URL 含 API key |
| 邊界處理 | 4 | 20% | Step PRE 檢查、部分失敗處理、URL 長度警告均到位 |
| **加權總分** | **2.95/5** | | |

---

*審核完成。裁決：⚠️ REVISE — 6bb5ebf 的 email→webhook 遷移在 pm.md:504 留下一處 `Chat Email` 殘留，導致 `/pm bye` 的 Google 同步靜默失效。更嚴重的是，CLAUDE.md 在 public repo 中 hardcode 了完整的 Webhook URL（含 API key 和 token），違反同一份文件宣告的安全編碼底線，且已暴露於公開網路。*
