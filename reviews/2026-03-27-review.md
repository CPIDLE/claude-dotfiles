# 紅隊審核報告

## Metadata

| 項目 | 值 |
|---|---|
| 日期 | 2026-03-27 |
| 專案 | claude-dotfiles |
| Branch | master |
| 審核 Commits | `980616d`, `4b34181`, `847887f` |
| 審核範圍 | CLAUDE.md, commands/bye.md, commands/hello.md, commands/pm.md, commands/sc.md, pm-v2-design.md |
| 審核員 | 獨立紅隊（Opus 4.6） |

---

## 總評：⚠️ REVISE

最大風險：**pm-v2-design.md 未完成 Slack → Google 遷移，多處殘留 Slack 術語，與 pm.md 的實際實作嚴重不一致**。設計文件宣稱是「重寫以反映當前狀態」（commit 847887f），但實際上資料流圖、同步流程、Dashboard 規則仍大量引用 Slack Canvas、Channel、Slack emoji 語法，這與 pm.md 已完成的 Google Workspace 遷移直接矛盾。按照設計文件操作會得到錯誤的架構理解。

---

## 自動檢查結果

| 檢查項 | 結果 |
|---|---|
| 測試 | ⚠️ 跳過（專案無測試框架） |
| Lint | ⚠️ 跳過（專案無 linter） |
| Debug 掃描 | ✅ 通過（唯一命中 `console.log` 在 pm.md:375，為 debug 掃描規則的文件描述，非實際 debug 程式碼） |
| Code Review | 見下方發現清單 |

---

## 統計表

|  | 🔴 阻塞 | 🟠 中等 | 🟡 輕微 |
|---|---|---|---|
| 指令定義檔 | 1 | 2 | 2 |
| 設計文件 | 1 | 2 | 1 |
| **合計** | **2** | **4** | **3** |

---

## 發現清單

### 🔴 阻塞（MUST FIX）

---

**F-001**
- **位置**：pm-v2-design.md:47-51（關鍵資料流圖）
- **嚴重度**：🔴
- **問題**：設計文件的核心資料流圖仍寫 `Slack Canvas（雙向同步）`、`#all-cpidle（Channel 摘要）`、`Dashboard Canvas（F0AMWD1GAD9）`，與 pm.md 已遷移至 Google Workspace 的實作完全矛盾。
- **驗證**：pm.md 已全面改為 Google Doc + Google Chat Space + Dashboard Google Doc（見 pm.md:263-338）。CLAUDE.md 關鍵 ID 表也已改為 `Google Chat Space` 和 `Dashboard Google Doc`。但 pm-v2-design.md 的架構圖仍指向 Slack 生態系，且 commit message 847887f 宣稱「rewrite pm-v2-design.md to reflect current state」。
- **裁決**：MUST FIX — 設計文件的核心架構圖與實作不符，直接誤導讀者。

---

**F-002**
- **位置**：pm-v2-design.md:115, 124, 132, 144（同步流程與 Dashboard 規則）
- **嚴重度**：🔴
- **問題**：設計文件的 `/pm sync` 同步流程和 Dashboard 規則仍使用 Slack 術語和 Slack 特有行為。具體殘留：
  - 第 115 行：選單選項仍寫「同步進度 → Slack Canvas + Channel（預設）」
  - 第 124 行：「Step B: Canvas 同步」
  - 第 132 行：「Canvas emoji 使用 Slack 語法（`:small_blue_diamond:` 非 `🔹`）」
  - 第 144 行：「Step 5: Slack 同步（Canvas + Channel + Dashboard）」
  - 第 126-131 行：Dashboard 規則仍描述 Slack Canvas 的 `replace`/`append` + `section_id` 行為（pm.md 已改為 Google Doc 的段落定位替換）
- **驗證**：pm.md 中 Dashboard 規則（pm.md:319-338）使用 `google_drive_fetch` + `## 🔹` header 定位 + Google Docs 寫入工具。pm.md:308 明確寫「Dashboard 使用標準 Unicode emoji」。設計文件第 132 行說「Canvas emoji 使用 Slack 語法」直接矛盾。
- **裁決**：MUST FIX — 設計文件的 Slack 特有行為描述（section_id、Slack emoji 語法）照做會完全失敗，因為目標已換成 Google Docs。

---

### 🟠 中等（SHOULD FIX）

---

**F-003**
- **位置**：commands/bye.md:127-128（Step 7）
- **嚴重度**：🟠
- **問題**：bye.md Step 7 指示「執行 `/pm sync` 選 1 的完整流程」，但 bye.md 缺少 pm.md 中的 Step PRE（Google 工具可用性檢查）。若 CLAUDE.md 中的 ID 仍為 placeholder，bye.md 的 Step 7 會直接呼叫 `/pm sync` 選 1，而 pm.md 的 Step PRE 會攔截，但 bye.md 的文件描述暗示會直接執行 Google Doc + Chat Space + Dashboard，沒有提及可能被跳過的情況。
- **驗證**：pm.md:238-256 定義了 Step PRE 的完整 placeholder 檢測邏輯。bye.md 完全沒有提及此檢查。若 agent 走 bye.md 路徑而非 pm.md 路徑（因為 bye.md 仍可獨立使用），graceful degradation 未被文件覆蓋。
- **裁決**：SHOULD FIX — bye.md 既標記為可獨立使用，其 Google 同步步驟就應至少提及 placeholder 檢查或直接引用 pm.md 的 Step PRE。

---

**F-004**
- **位置**：commands/sc.md:16（遷移對照表）
- **嚴重度**：🟠
- **問題**：sc.md 的舊指令 → 新指令對照表中，`/sc 2` 映射為「`/pm sync`（選單中選擇）」，但舊 `/sc 2` 的功能是「同步 Canvas → 本地」（即反向同步），而新 `/pm sync` 選單中根本沒有反向同步選項。pm.md 的同步流程只有「本地 → Google Doc」單向更新。
- **驗證**：舊 sc.md（diff 中可見刪除的 Step 2A 選 2）功能為「📥 同步 Canvas → 本地」。pm.md:263-267 的 Step B 只定義了本地 → Google Doc 的單向流程。pm.md 整份文件中無任何 Google Doc → 本地的反向同步定義。
- **裁決**：SHOULD FIX — 對照表暗示 `/sc 2` 有對應替代，但實際上該功能已被移除。應明確標示為「功能已移除」或說明替代方案。

---

**F-005**
- **位置**：commands/pm.md:249（Step PRE 安裝指引）
- **嚴重度**：🟠
- **問題**：Step PRE 的安裝指引寫「安裝 Google Workspace MCP（npm install -g @googleworkspace/cli）」，同一指引在 pm.md:645 重複出現。`@googleworkspace/cli` 這個 npm 套件名是否真實存在未經驗證。若為虛構套件名，使用者照做會安裝失敗。
- **驗證**：此套件名在 npm registry 上無法透過本地工具驗證。但從命名慣例看，Google 官方 MCP 套件不太可能使用此名稱（Google 通常使用 `@google-cloud/` 或 `@googleapis/` namespace）。Claude Code MCP 的 Google Workspace 整合通常透過 settings.json 中的 MCP server 設定，而非 npm 全域安裝。
- **裁決**：SHOULD FIX — 安裝指引應確認為真實可用的套件名與安裝步驟。錯誤的安裝指引會浪費使用者時間。

---

**F-006**
- **位置**：pm-v2-design.md:178-179（檔案結構）
- **嚴重度**：🟠
- **問題**：設計文件的檔案結構宣稱 pm.md 為「~640 行」，實際為 656 行（經 `wc -l` 驗證）。此為小偏差，但同區段宣稱安裝腳本為 `install.ps1` 和 `install.sh`，需確認這些檔案是否存在於 repo 中。
- **驗證**：`wc -l` 結果為 656 行。行數偏差為 16 行（2.5%），在文件描述中使用「~640 行」有些誤導。
- **裁決**：SHOULD FIX — 如果要標行數，應更新為接近實際的數字。

---

### 🟡 輕微（NICE TO FIX）

---

**F-007**
- **位置**：commands/hello.md:115（正常模式提示語）
- **嚴重度**：🟡
- **問題**：hello.md 的提示語為「工作中可用 /pm sync 同步進度，收工用 /pm bye」。但 hello.md 自身的 legacy notice（第 1 行）建議改用 `/pm` 或 `/pm new`。作為 legacy 指令，hello.md 在正常模式下提示使用者用 `/pm sync` 和 `/pm bye`，卻不提及其自身的替代指令 `/pm`，訊息邏輯不一致。
- **驗證**：hello.md 第 1-2 行：「建議改用 `/pm` 或 `/pm new`」。第 115-117 行提示 `/pm sync` 和 `/pm bye` 但不提及 `/pm` 本身。
- **裁決**：NICE TO FIX — 既然已標記 legacy，在內部仍導流到 `/pm` 子命令而非統一入口，不影響功能但不夠一致。

---

**F-008**
- **位置**：pm-v2-design.md:105-107（Status Line 已知限制）
- **嚴重度**：🟡
- **問題**：設計文件的「已知限制」第三點寫「`pm-update.sh` 的 sed 替換只能更新已存在的 key」，但本次審核範圍的 commits 中未包含 `pm-update.sh` 本身，無法交叉驗證此宣稱是否仍然成立。
- **驗證**：pm-update.sh 不在本次 diff 範圍內，此為文件宣稱但無法在本次審核中驗證的項目。
- **裁決**：NICE TO FIX — 設計文件的已知限制應定期與實際程式碼同步驗證。

---

**F-009**
- **位置**：pm-v2-design.md:212-214（已知限制與待辦）
- **嚴重度**：🟡
- **問題**：已知限制第四點「Deep review 的 subagent 尚未大量測試」，但 reviews/ 目錄已有一份 deep review 報告（2026-03-26-23-30.md），且開發歷程 Phase 6 提及「紅隊審核報告：修正 F-007/F-008/F-009」，表示至少已跑過一次。「尚未大量測試」的表述模糊，沒有定義「大量」的標準。
- **驗證**：`reviews/2026-03-26-23-30.md` 存在（12111 bytes），Phase 6 記錄了修正動作。
- **裁決**：NICE TO FIX — 若已有成功的測試案例，應更新限制描述（如「僅測試過 N 次」）。

---

## 未覆蓋風險

1. **Google Chat MCP 工具的具體 API**：pm.md 多次提及「Google Chat MCP 工具搜尋 Space」，但未指定具體的 tool name（不像 Slack 時期有明確的 `slack_search_public`、`slack_send_message` 等）。Agent 執行時需自行推斷正確的 MCP tool call。
2. **Google Docs 寫入工具不存在於當前 MCP 配置**：從系統可用的 MCP tools 來看，有 `google_drive_search` 和 `google_drive_fetch`（唯讀），但無 Google Docs 寫入工具。pm.md 的 Step B/D 假設存在「Google Workspace MCP 的 Docs 寫入工具」，但當前環境中此工具不存在。整個 Google 同步功能在目前配置下會全部 graceful skip。
3. **bye.md 與 pm.md bye 的步驟編號不一致**：bye.md 有 Step 0-9（10 步），pm.md bye 有 Step 0-8（9 步）。bye.md 的 Step 7 = Google 同步，pm.md bye 的 Step 5 = Google 同步。如果使用者混用，步驟對照會混亂。
4. **install.ps1 / install.sh 未在本次審核範圍**：設計文件提及這些安裝腳本，但未包含在 diff 中，無法確認它們是否也需要 Slack → Google 的遷移更新。

---

## 評分

| 面向 | 分數 (1-5) | 權重 | 加權 |
|---|---|---|---|
| 內部一致性 | 2 | 30% | 0.60 |
| 可行性（照做能跑） | 3 | 25% | 0.75 |
| 遷移完整性 | 2 | 25% | 0.50 |
| 文件品質 | 3 | 10% | 0.30 |
| 邊界處理 | 3 | 10% | 0.30 |
| **加權總分** | | | **2.45 / 5** |

- **內部一致性（2/5）**：pm-v2-design.md 與 pm.md 在同步流程、emoji 語法、資料流上嚴重矛盾。
- **可行性（3/5）**：pm.md 本身的指令定義可行（graceful skip 設計合理），但設計文件照做會壞。
- **遷移完整性（2/5）**：pm.md、bye.md、hello.md、sc.md、CLAUDE.md 遷移完成，但 pm-v2-design.md 大量殘留未遷移。
- **文件品質（3/5）**：pm.md 結構清晰，但 npm 套件名未驗證、安裝步驟可能錯誤。
- **邊界處理（3/5）**：pm.md 的 Step PRE placeholder 檢查和工具不可用 graceful skip 設計良好，但 bye.md 獨立路徑未覆蓋。
