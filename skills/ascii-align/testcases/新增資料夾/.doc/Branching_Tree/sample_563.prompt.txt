# _sample_563.md

## Input — Code Block

```
  痛點                                    解法

  TMflow 原廠 Jog Panel                  統一 UI + 外接 HID
  ┌──────────────────────┐                 ┌───────────────────────────┐
  │ 按鈕小、手離工件     │                 │ 手不離工件 .  眼不離對準   │
  │ 視線在螢幕<->工件切換 │     ─────>      │                           │
  │ 每次教點：點滑鼠     │                 │ 搖桿 / SpaceMouse / 踏板  │
  │ 效率低、易出錯       │                 │ 教點速度提升 30%+         │
  └──────────────────────┘                 └───────────────────────────┘

  支援裝置：
  ┌───────────────┬──────────────────┬──────────────────┬───────────────┐
  │ Xbox/DualShock│ 3Dconnexion      │ 三踏板腳踏開關   │ 自製 HID      │
  │ 雙類比搖桿    │ SpaceMouse       │                  │ 面板          │
  ├───────────────┼──────────────────┼──────────────────┼───────────────┤
  │ 左桿=平移     │ 6-DOF 連續 Jog   │ Deadman          │ Teensy/QMK    │
  │ 右桿=旋轉     │ 最直覺           │ FreeDrive 切換   │ 完全自定義    │
  │ D-pad=Step    │                  │ 一鍵存點         │               │
  │ 按鈕=存點     │                  │                  │               │
  └───────────────┴──────────────────┴──────────────────┴───────────────┘

  教點工作流：
  1  Free Drive 粗定位 --> 2  Step Jog 精調 (0.1mm) --> 3  一鍵存點
  --> 4  閉環視覺校正 (<=0.05mm) --> 5  自動同步 GYRO YAML + Git

```

## Issues — Code Block

**Symbol replacements applied:**
  L5: `·` (MIDDLE DOT)
  L6: `↔` (LEFT RIGHT ARROW)
  L6: `→` (RIGHTWARDS ARROW)
  L23: `①` (CIRCLED DIGIT ONE)
  L23: `→` (RIGHTWARDS ARROW)
  L23: `②` (CIRCLED DIGIT TWO)
  L23: `→` (RIGHTWARDS ARROW)
  L23: `③` (CIRCLED DIGIT THREE)
  L24: `→` (RIGHTWARDS ARROW)
  L24: `④` (CIRCLED DIGIT FOUR)
  L24: `≤` (LESS-THAN OR EQUAL TO)
  L24: `→` (RIGHTWARDS ARROW)
  L24: `⑤` (CIRCLED DIGIT FIVE)

**Width mismatches (need fixing):**
  Group L4-L9 target=72:
    L5: w=73 (+1)
    L6: w=73 (+1)
